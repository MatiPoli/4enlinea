Para realizar los tests es necesario tener instalado pytest.

Una vez instalado, ejecutamos en la consola el comando 'python -m py.test -s'
e ingresamos la siguiente secuencia: '1,2,3,4,5,6,1' para que los test creados
corran correctamente.